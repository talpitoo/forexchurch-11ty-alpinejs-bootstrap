<div class="relative">
    <div class="swiper mx-n4" data-forexchurch-swiper>
        <div class="swiper-wrapper">
            {% for slide in slidesData %}
                <div class="swiper-slide px-4 pb-4">
                    <div class="forexchurch-tile d-flex flex-column break-inside-avoid-column">
                        <figure>
                            <div class="{{ slidesHeight }}">
                                {# NOTE: example of setting an individual pictureObject before including components/picture-example.njk #}
                                {% set pictureObject = {
                                    'webP': slide.webP,
                                    'webP2x': slide.webP2x,
                                    'jpg': slide.jpg,
                                    'jpg2x': slide.jpg2x
                                } %}
                                {% include "components/picture-example.njk" %}
                            </div>
                        </figure>
                        <button type="button" class="btn btn-primary w-100">
                            Visit Broker
                        </button>
                        <button type="button" class="btn btn-outline-primary w-100">
                            Read Review
                        </button>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

    <button type="button" class="btn btn-link text-tertiary swiper-button-prev">
        <svg class="" aria-hidden="true" width="24" height="24">
            <use href="/img/icons/symbol/svg/sprite.css.svg#arrow-left"></use>
        </svg>
        <span class="visually-hidden">previous slide</span>
    </button>
    <button type="button" class="btn btn-link text-tertiary swiper-button-next">
        <span class="visually-hidden">next slide</span>
        <svg class="" aria-hidden="true" width="24" height="24">
            <use href="/img/icons/symbol/svg/sprite.css.svg#arrow-right"></use>
        </svg>
    </button>
</div>