<section class='py-section'>
    <div class='container container-max-xl'>
        <div class="row">
            <div class="col-lg-10 offset-lg-1 text-center">
                <h2>Side By Side Forex Broker Comparison</h2>
                <p class="mb-24px">On this page you can conduct a side-by-side Forex Broker Comparison. Simply select up to 3 Forex Brokers from the drop down menus to compare features for each broker simultaneously. To view more detailed information, visit each brokers detail page.</p>
                <a href="#" class="btn btn-primary mb-5">
                    <span>Compare Brokers</span>
                    <svg class="ms-1" aria-hidden="true" width="24" height="24">
                        <use href="/img/icons/symbol/svg/sprite.css.svg#arrow-right"></use>
                    </svg>
                </a>
            </div>
        </div>

        {# <div x-data="{ test: 'Alpine.js is working!' }" x-text="test" class='bg-tertiary-light p-5 rounded-4 mb-5'></div> #}

        {# <div x-data="loadBreweries()" x-init="getBreweries"> #}
        <div x-data="loadBreweriesMock()" x-init="getBreweriesMock">
            <div class="table-responsive">
                <table class="table table-striped w-full">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Type</th>
                            <th>City</th>
                            <th>State</th>
                        </tr>
                    </thead>
                    <tbody>
                        <template x-for="brewery in breweries" :key="brewery.id">
                            <tr>
                                <td x-text="brewery.name"></td>
                                <td x-text="brewery.brewery_type"></td>
                                <td x-text="brewery.city"></td>
                                <td x-text="brewery.state"></td>
                            </tr>
                        </template>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>